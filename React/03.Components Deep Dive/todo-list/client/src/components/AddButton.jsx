export default function AddButton() {
    return (
        <div className="add-btn-container">
            <button type="submit" className="btn">
                + Add new Todo
            </button>
        </div>
    );
}
